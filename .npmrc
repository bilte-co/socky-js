registry=https://registry.npmjs.org/
# package.json declares aube as the packageManager. The release
# workflow ends up shelling out to `npm publish` via changesets, and
# npm 10+ enforces the packageManager field unless this is disabled.
package-manager-strict=off
