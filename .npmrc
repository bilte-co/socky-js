registry=https://registry.npmjs.org/
# package.json declares aube as the packageManager. aube is
# pnpm-lockfile compatible, so we let pnpm run too (it is used by
# the release workflow to publish via changesets).
package-manager-strict=false
